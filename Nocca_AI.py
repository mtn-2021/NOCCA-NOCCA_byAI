class Nocca_AI(object):

    def select_stone(self, board: list[list[list]], stone: list[tuple[int,int]]) -> int:
        return map(int,stone[0])

    def select_direction(self, direction: list)-> str:
        return str(7)
